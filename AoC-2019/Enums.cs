namespace CleanCode
{
    public enum MultiAmpMode
    {
        Single,
        Feedback,
    }
    
    public enum RefValue
    {
        Reference,
        Value,
    }

    public enum ParameterMode
    {
        Position,
        Intermediate,
    }
}